# Dependencies
node_modules/

# Runtime workspace
workspace/

# Environment variables
.env

# Test caches
__tests__/soak/.model-cache/

# Python cache
__pycache__/
*.pyc
*.pyo
*.pyd

# Migration build output
scripts/export-tencent-vdb/dist/
scripts/migrate-sqlite-to-tcvdb/dist/
scripts/read-local-memory/dist/

# Root-level plugin build output — COMMITTED because pi installs this package as a
# git dependency with `npm install --omit=dev`, so tsdown (a devDependency) can
# never run at install time. dist/index.mjs is what pi loads at runtime; rebuild
# (npm run build:plugin) and commit it alongside every source change.

node_modules/
benchmark-runs/
.codebuddy
.ai_assets

# Dev-only scripts & lockfile (not shipped in MR)
install-plugin.sh
package-lock.json
pnpm-lock.yaml
yarn.lock
test-offload.sh
test-offload-mmd.sh
test-offload-sessions.sh

# npm pack / release tarballs (never commit packaged outputs)
*.tgz
*.tar.gz

# log files
*.log
# roborev snapshots
/.roborev/
